# Progress

Current: Phase 8 — M7 UI

## Phase 0 — Understand and prepare
Status: committed (4504609)

### Tasks

- [x] P0.1 Read the complete contract and design package — Acceptance: all required documents and 29 artboards inspected — Verify: reading order reconciled against `AGENT_KICKOFF_PROMPT.md` — Result: complete; no contract files changed.
- [x] P0.2 Toolchain preflight — Acceptance: macOS/Xcode/Rust/Node meet minimums and both Rust targets are installed — Verify: every mandated version/target command recorded in `UNDERSTANDING.md` — Result: pass; installed `x86_64-apple-darwin` without sudo or GUI.
- [x] P0.3 Write implementation understanding — Acceptance: product, sources, update path, windows/routes, settings, 13 answers and all verification items are covered with references — Verify: manually cross-checked against cited sections — Result: complete.
- [x] P0.4 Prepare repository records — Acceptance: Git initialized; required ignore rules, progress and append-only decision log exist — Verify: `git status --short --ignored` — Result: complete.
- [x] P0.5 Review and commit — Acceptance: only allowed Phase 0 files changed and every answer matches its citation — Verify: staged diff review, `git diff --cached --check`, 29-artboard count, `git log -1 --stat`, clean status — Result: review passed; commit follows this record.

### Review

- Checks: documentation-only phase; toolchain preflight ✅
- Diff review: all implementation notes re-read; corrected the Claude bridge source table to avoid claiming a plan field and chose a neutral full-opacity widget default rather than copying sample UI data. Immutable contract/design files are staged as the initial repository baseline, not modified.
- Decisions: D-001 · ⚠ VERIFY: identified and routed to fixture/fake tests or handover
- New handover items: live Codex account check; old-Codex compatibility check; post-v1 OAuth shape; native Reduce Transparency; §14.4; 8-hour soak; Safari memory; native design; real log grep; `/Applications` smoke; optional signing/notarization

### Blocked (hard stops only)

- None.

## Phase 1 — Scaffold (M0)
Status: committed (090fbf1)

### Tasks

- [x] T0.1 Workspace — Goal: establish the linted Rust workspace and three empty crates — Files: root `Cargo.toml`, `rust-toolchain.toml`, `.cargo/config.toml`, `crates/{usage-core,usage-sources,statusline-bridge}/**` — Acceptance: workspace members and off-by-default `claude-oauth` feature match §13 M0 — Verify: `cargo build --workspace` succeeds with zero warnings — Result: split into two ≤8-file batches; `cargo build --workspace` ✅ (4 crates, zero warnings).
- [x] T0.2 Tauri + UI — Goal: scaffold Tauri 2 with React 19/strict TypeScript and the sidecar build hook — Files: `ui/**`, `src-tauri/**`, `scripts/build-sidecar.sh` — Acceptance: exact three-window shell configuration, strict TS/ESLint, typed npm scripts, and native sidecar naming are wired — Verify: `npm --prefix ui run typecheck`, `npm --prefix ui run lint`, `cargo tauri dev` opens a window — Result: official generator 4.7.4 used as the baseline; React 19.3.0, TypeScript 5.9.3, Tauri CLI 2.11.5 installed; typecheck ✅; lint ✅; `cargo tauri dev` ✅ (sidecar built, Vite ready, native process launched and remained live; unbundled debug app was not addressable by the desktop UI inventory).
- [x] T0.3 Decisions — Goal: record the three mandated foundational choices — Files: `docs/DECISIONS.md` — Acceptance: ts-rs, macOS 26, and deferred OAuth entries include context/rule/reason/evidence — Verify: manual review against §4 and §13 M0 — Result: D-002 through D-004 added and cross-checked ✅.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · `cargo clippy --workspace --all-targets -- -D warnings` ✅ · `cargo test --workspace` ✅ · UI typecheck ✅ lint ✅ build ✅ · `cargo tauri dev` ✅
- Diff review: all scaffold files read in full; generated dependency locks retained; generated schemas, sidecars, dependencies and build output ignored; no contract/design edits; no forbidden Rust constructs, secrets, unbounded resources or unrelated changes. Fixed root release-profile placement, added build-script docs, corrected Tauri command working directories, and scoped type-aware ESLint to TS modules.
- Decisions: D-002, D-003, D-004 · ⚠ VERIFY: none in M0
- New handover items: none; native debug-window inventory limitation is a development-tooling observation, not an acceptance blocker

### Blocked (hard stops only)

- None.

## Phase 2 — Core domain (M1)
Status: committed (7163e95)

### Tasks

- [x] T1.1 Domain types and bounded logging — Goal: implement the strict, documented §7.1 domain model, validated unit newtypes, and UTF-8-safe external-text truncation — Files: `crates/usage-core/src/{lib.rs,domain.rs}` plus focused tests — Acceptance: `Percent` rejects non-finite input and clamps finite values; all IPC fields follow the prescribed JSON/TypeScript representations; `log_trunc` returns at most 2 KiB without splitting a character — Verify: red/green unit tests for NaN, infinities, 120, −1, short text, ASCII boundary, and multibyte boundary — Result: 5 focused tests pass; T1.1 clippy is warning-free.
- [x] T1.2 Window classification — Goal: implement the pure duration classifier — Files: `crates/usage-core/src/window.rs` plus focused tests — Acceptance: session, weekly, other, absent/hinted, negative, and oversized durations follow §7.2 without lossy casts — Verify: 5 boundary-focused tests pass.
- [x] T1.3 TypeScript bindings — Goal: generate reproducible `ts-rs` exports for every IPC type — Files: `ui/src/bindings/**` plus export contract test — Acceptance: every IPC type is exported; 64-bit JSON values use TypeScript `number` — Verify: `cargo test -p usage-core export_bindings` runs 17 export tests; `UnixSeconds.ts` contains `number` and no `bigint`.
- [x] T1.4 Source-state merge — Goal: implement §7.4 ingest and derive semantics without I/O — Files: `crates/usage-core/src/merge.rs` and its tests — Acceptance: rules I1–I4 and D1–D5, races (a)–(e), deterministic source selection, rollover/refinement/reset handling, and at least five-event permutation invariance are covered — Verify: 15 named tests pass, including every rule, all required races, and all 120 orderings of five full-reading events; focused clippy is warning-free.
- [x] T1.5 History — Goal: aggregate bounded per-source token history and produce correctly scoped 24-hour/7-day views — Files: `crates/usage-core/src/history.rs` and its tests — Acceptance: exact zero-filled series, source isolation, Codex account/fallback selection, UTC account days, dedupe, 8-day pruning, and DST behavior match §7.5 — Verify: 7 deterministic tests pass, including the 25-hour `America/New_York` day on 2026-11-01; storage metrics prove hour/dedupe pruning; focused clippy is warning-free.
- [x] T1.6 Projection and alerts — Goal: implement weekly pace projection and stateful threshold/reset alerts — Files: `crates/usage-core/src/{projection.rs,alerts.rs}` and tests — Acceptance: projection depends only on weekly percentage/timing; alerts fire only on upward crossings, dedupe per reset, re-arm after reset, and prune expired state — Verify: 5 focused tests pass for weekly-only projection, six-hour suppression, upward/downward transitions, reset re-arming, reset notification, and bounded firing state; focused clippy is warning-free.

### Phase review and commit

- [x] Goal: leave M1 clean, documented, generated, and warning-free — Files: Phase 2 changes only — Acceptance: all M1 acceptance criteria pass; no forbidden constructs or unrelated edits; coverage meets the project gate — Verify: fmt, clippy, workspace tests, binding diff check, UI checks, coverage measurement, staged diff review, commit, `git log -1 --stat`, clean status — Result: all automated gates through the pre-commit review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · workspace clippy with `-D warnings` ✅ · workspace tests ✅ (58 `usage-core` tests, none skipped) · `cargo llvm-cov -p usage-core --fail-under-lines 90` ✅ (95.89% lines; domain/window/projection 100%, merge 97.19%) · generated-binding SHA-256 regeneration check ✅ · UI typecheck/lint/build ✅ · `git diff --cached --check` ✅.
- Diff review: every M1 source and test file re-read; the highest-risk merge paths have one test per I1–I4/D1–D5 plus five named races and all 120 permutations. History never mixes sources, retains bounded state, preserves UTC account days and passes a 25-hour DST fixture. All source files stay below 400 lines after splitting time helpers and merge test groups. No I/O entered `usage-core`; no non-test unwrap/expect, unsafe, secrets, unbounded collections, contract edits, or unrelated files were introduced.
- Decisions: D-005, D-006. The review rejected ts-rs's heavyweight formatter graph (57 packages) and retained a std-only deterministic generated-whitespace normalizer instead.
- New handover items: none; M1 is fully fixture/pure-logic verifiable.

### Blocked (hard stops only)

- None.

## Phase 3 — Parsers (M2)
Status: committed (bf538a9)

### Tasks

- [x] T2.1 Codex app-server parser — Goal: convert lenient camelCase RPC DTOs into strict full/sparse readings while enforcing the `codex` limit-ID filter — Files: `crates/usage-core/src/parse/{mod.rs,codex_app_server.rs}`, focused tests, Appendix A.2 fixture — Acceptance: the documented response yields exactly one full Weekly 2% reading with plan `Pro`; a secondary-only notification is one-window partial; `premium` never reaches the store — Verify: red/green fixture and sparse/filter tests — Result: 4 focused tests pass; the exact Appendix A.2 response yields one full Weekly 2% `Pro` reading, sparse secondary data remains partial, and both mapped and notification `premium` snapshots are excluded; full fmt, strict workspace Clippy, and workspace tests pass.
- [x] T2.2 Codex rollout parser — Goal: parse snake_case JSONL into full limit readings and fork-safe token deltas — Files: `crates/usage-core/src/parse/codex_rollout.rs`, focused tests, Appendix A.1/A.6 fixtures — Acceptance: A.1 yields Weekly 1% plus its delta; premium yields tokens only; legacy `resets_in_seconds` computes 1790132400; forked sessions count exactly 3000 under all four §2.2 rules — Verify: fixture-driven red/green tests — Result: 4 focused tests pass; the exact fixtures yield the documented full reading, 211,554 first-line delta, 1,790,132,400 legacy reset, and fork-safe deltas `[1000, 0, 2000]`; an added counter-reset case proves rule 3, and a `premium` case proves token events survive the limit filter; full task gates pass.
- [x] T2.3 Claude status-line parser — Goal: convert the bridge-file envelope and lenient Claude rate-limit shape into full readings — Files: `crates/usage-core/src/parse/claude_statusline.rs`, focused tests, Appendix A.4 fixture — Acceptance: A.4 yields Session 23.5 and Weekly 41.2; an absent `five_hour` yields only Weekly — Verify: exact fixture and missing-window tests — Result: 4 focused tests pass; A.4 yields the exact Session/Weekly values, independently missing windows are omitted, empty shapes are ignored, and the parser accepts both raw `rate_limits` input and the persisted `rateLimits` envelope while honoring `writtenAt`; full task gates pass.
- [x] T2.4 Claude local-log parser — Goal: extract assistant token events without accepting non-assistant records or losing dedupe provenance — Files: `crates/usage-core/src/parse/claude_log.rs`, focused tests, Appendix A.3 fixture — Acceptance: A.3 totals 92,586 tokens and produces `message_id:request_id`; non-assistant lines are skipped — Verify: exact fixture and negative tests — Result: 4 focused tests pass; the exact duplicate fixture emits identical 92,586-token events with `msg_01TEST:req_01TEST`, the history store counts them once, non-assistant/no-usage records are skipped, missing counters default to zero, and overflow is rejected; full task gates pass.

### Phase review and commit

- [x] Goal: leave M2 lenient at external boundaries and strict internally — Files: Phase 3 changes only — Acceptance: every Appendix fixture is checked in verbatim, malformed/unknown fields cannot panic, parser output observes unit/source/filter contracts, and coverage remains above the core gate — Verify: fmt, workspace clippy/tests, UI checks if bindings change, coverage, staged diff review, commit, `git log -1 --stat`, clean status — Result: all automated gates through the pre-commit review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · workspace Clippy with `-D warnings` ✅ · workspace tests ✅ (17 parser-focused/robustness tests; none skipped) · `cargo llvm-cov -p usage-core --fail-under-lines 90` ✅ (95.53% lines; every provider parser ≥96%) · generated bindings unchanged ✅ · `git diff --check` ✅.
- Diff review: all parser sources and tests re-read; every external DTO field is optional with serde defaults and unknown fields remain accepted. Appendix A.1–A.4 and A.6 files compare byte-for-byte with their documentation code blocks. Codex limit IDs are filtered before readings, token deltas remain independent of that filter and per-file state is explicit. Claude duplicates retain a stable key for store dedupe, arithmetic is checked, malformed JSON returns typed errors, and no parser performs I/O, logs secrets, panics, or exceeds 400 lines.
- Decisions: D-007 records the raw-status-line versus persisted-bridge shape ambiguity and the additive parser behavior.
- New handover items: none; M2 is fully fixture/pure-logic verifiable.

### Blocked (hard stops only)

- None.

## Phase 4 — Status-line bridge (M3)
Status: committed (c0ccd5b)

### Tasks

- [x] T3.1 Synchronous bridge fast path — Goal: accept bounded Claude status-line stdin, atomically persist the documented schema-1 bridge file, and emit the compact fallback text without ever failing Claude Code — Files: `crates/statusline-bridge/{Cargo.toml,src/**,tests/basic.rs}` (split to keep resource and process logic small) — Acceptance: only `serde_json` and `tempfile` are runtime dependencies; input is capped at 4 MiB; a temp `--out-dir` run prints `5h 24% · 7d 41%`, writes valid complete JSON with verbatim rate limits, and exits 0; invalid/oversized input exits 0 silently — Verify: test-first binary integration coverage, the exact §13 command in a temp directory, and standard workspace gates — Result: 2 binary tests pass; the exact temp probe prints `5h 24% · 7d 41%`, `jq` validates the schema/session/time and verbatim window object, invalid and >4 MiB inputs exit 0 silently, atomic writes use unique same-directory temp files plus `sync_all`, and full fmt/strict Clippy/workspace tests pass.
- [x] T3.2 Chained status line — Goal: forward the original stdin to an existing configured command, return its stdout verbatim, and kill it at the hard two-second deadline after the bridge file is already durable — Files: bridge process module and focused tempdir tests — Acceptance: `cat >/dev/null; echo CHAINED` returns `CHAINED`; `sleep 5` finishes in 2.0–2.3 seconds with the rate-limit file present; child stdio cannot deadlock — Verify: red/green integration tests, the two §13 shell probes, and standard workspace gates — Result: 2 focused chain tests pass; the exact command returned `CHAINED`, the direct-binary sleep probe returned in 2.01 s, and `jq` confirmed the bridge file was already valid; stdin/stdout workers avoid pipe deadlock, stderr is discarded, timeout kills/reaps the child, and full task gates pass.
- [x] T3.3 Overlap safety and performance — Goal: prove atomic last-writer-wins behavior, abandoned-temp cleanup, and the unchained latency budget under realistic process concurrency — Files: `crates/statusline-bridge/tests/{concurrency.rs,budget.rs}` plus minimal production fixes — Acceptance: 50 overlapping invocations with every fifth killed leave a valid target; startup cleanup removes stale `.tmp*`; 100 release runs have p95 <20 ms — Verify: debug concurrency test, `cargo test --release -p statusline-bridge`, and standard workspace gates — Result: synchronized 50-process overlap with 10 varied kill attempts leaves a valid schema-1 target and at most one temp per killed writer; future-time startup cleanup removes all abandoned temp files; 100 optimized invocations pass the p95 <20 ms assertion; release and full workspace gates pass.

### Phase review and commit

- [x] Goal: leave M3 synchronous, bounded, atomic, and isolated from real user paths — Files: Phase 4 changes only — Acceptance: no async runtime or extra dependency, no real Application Support access in verification, no panic/output on failure, and all child/temp resources are reaped or cleaned — Verify: fmt, strict workspace Clippy/tests, release M3 tests, dependency/diff/resource review, commit, `git log -1 --stat`, clean status — Result: all automated gates through the pre-commit review pass; commit follows this record.

### Review

- Checks: exact fast-path/invalid/chain/sleep temp probes ✅ · `cargo fmt --all -- --check` ✅ · workspace Clippy with `-D warnings` ✅ · workspace tests ✅ (6 M3 integration tests, none skipped) · `cargo test --release -p statusline-bridge` ✅ (100-process p95 <20 ms assertion, 50-process overlap, process-group timeout) · direct dependency tree exactly `serde_json` + `tempfile` ✅ · `git diff --check` ✅.
- Diff review: every M3 source/test file re-read; stdin is capped at 4 MiB, rate limits are copied semantically unchanged into a unique same-directory temp file, `sync_all` precedes atomic persistence, startup cleanup targets only hour-old `.tmp*` regular files, and all verification paths use tempdirs. Review caught and fixed an unbounded chained-stdout `Vec` and shell-only timeout: stdout now spools to an anonymous temp file, and the whole isolated process group is killed/reaped before the 2.3 s ceiling. No async runtime, panic/unwrap/unsafe in production, real user-data access, secret output, leaked child, or file over 400 lines remains.
- Decisions: D-008 resolves the broad `dirs` table versus M3's explicit two-dependency rule in favor of the task-specific contract.
- New handover items: none; native installer consent and real Claude effectiveness remain later M5/manual acceptance work, not M3 bridge gaps.

### Blocked (hard stops only)

- None.

## Phase 5 — I/O infrastructure (M4)
Status: committed (6175214)

### Tasks

- [x] T4.1 Paths, events, and store actor — Goal: centralize all derived filesystem paths and route bounded source events through the sole mutable usage/history owner — Files: `crates/usage-sources/{Cargo.toml,src/{lib.rs,paths.rs,event.rs,store.rs},tests/**}` — Acceptance: overrides/environment/default roots resolve without touching real data in tests; the source channel is capacity 256 and alert channel 32; readings derive through a watch snapshot, history commands reply, and cancellation ends the actor within 100 ms — Verify: red/green temp-root and actor tests plus standard workspace gates — Result: 4 focused tests pass; synthetic roots prove settings > environment > default path resolution, bounded channel constants are 256/32, readings/history/75% alerts traverse the actor, and idle cancellation completes within 100 ms; a package-scoped clean removed a stale pre-M3 test binary and a cold full fmt/strict Clippy/workspace test run passes.
- [x] T4.2 Bounded incremental tailer — Goal: read appended JSONL bytes exactly once while surviving partial lines, truncation/rotation, oversized records, and stale file-state pruning — Files: `crates/usage-sources/src/tail.rs` and focused tempdir tests — Acceptance: appends emit once; partial data completes on the next append; shorter files reset offset; >16 MiB lines are skipped without unbounded buffers; file state older than 8 days is forgotten — Verify: listed red/green tests plus standard workspace gates — Result: 5 focused tempdir tests pass; append/partial delivery is exactly-once, both truncation and inode replacement reset state, oversized lines discard through the next newline while retaining at most 16 MiB per file, complete output batches cap at 4,096 lines/~16 MiB and continue without loss, and modification state older than eight days is pruned; full fmt, strict workspace Clippy, and workspace tests pass.
- [x] T4.3 Debounced filesystem watcher — Goal: adapt `notify` callbacks into a bounded async path stream with recursive watching, relevant-event filtering, and 500 ms per-path coalescing — Files: `crates/usage-sources/src/watch.rs` and focused tempdir tests — Acceptance: a tempdir write yields one path event, bursts coalesce per path, watcher ownership ends on cancellation — Verify: red/green FSEvents test plus standard workspace gates — Result: 2 native FSEvents tempdir tests pass outside the command sandbox; bounded 256-entry callback/output channels and a 256-path debounce set coalesce write bursts to one canonical path, JSONL/exact-file filters reject irrelevant events, and cancellation drops watcher ownership within 100 ms; full fmt, strict workspace Clippy, and workspace tests pass.
- [x] T4.4 Adaptive scheduler — Goal: provide per-source due times for activity-aware polling, immediate triggers, minimum gaps, capped backoff, and wake detection — Files: `crates/usage-sources/src/scheduler.rs` and paused-time tests — Acceptance: active/idle intervals, 15 s minimum gap, 30 s→30 min capped backoff with bounded jitter/Retry-After, UI-visible freshness, and >60 s wall/monotonic wake drift follow §§3.3/9.4 — Verify: deterministic `tokio::time::pause()` tests plus standard workspace gates — Result: 5 paused-time/timing tests pass; Codex switches 600 s idle ↔ 120 s active, triggers retain the 15 s floor, failures double from 30 s to a 30 min cap with ±20% jitter and longer Retry-After precedence, stale visible UI reads immediately, >60 s wall/monotonic drift triggers wake detection, and cancellation is prompt; `SourceEvent::Activity` is routed store→scheduler through bounded actor messages; full fmt, strict workspace Clippy, and workspace tests pass.

### Phase review and commit

- [x] Goal: leave M4 bounded, cancellation-safe, deterministic under tests, and ready for concrete sources — Files: Phase 5 changes only — Acceptance: no unbounded channels/buffers, blocking filesystem work on async tasks, leaked watchers/tasks, real-user path access, or duplicate delivery — Verify: fmt, strict workspace Clippy/tests, dependency/resource/diff review, commit, `git log -1 --stat`, clean status — Result: all automated gates through the pre-commit review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · workspace Clippy with `-D warnings` ✅ · workspace tests ✅ (17 M4 integration tests; none skipped) · native FSEvents tempdir coverage ✅ outside the command sandbox · UI typecheck/lint/build ✅ · direct dependency tree matches §4.1 ✅ · `git diff --check` ✅.
- Diff review: every M4 source/test file was re-read; paths have one derivation point and tests use synthetic roots, the store/scheduler remain single-owner actors, all mpsc queues and watcher pending state are bounded, every long-lived loop is cancellation-aware, and native watcher ownership stays inside its source task. Review caught an otherwise unbounded vector of completed tail lines: each call now caps both bytes and lines, exposes `has_more`, and a 5,000-line test proves continuation without loss. Partial lines remain capped at 16 MiB; source files remain below 400 lines; production code contains no unwrap/expect/panic/unsafe, real-user test access, leaked task, or unbounded collection.
- Decisions: D-009 records settings > environment > defaults path precedence.
- New handover items: none; native FSEvents behavior was verified on the host rather than replaced with a polling fake.

### Blocked (hard stops only)

- None.

## Phase 6 — Concrete sources (M5)
Status: committed (d8198cc)

### Tasks

- [x] T5.1 Codex JSON-RPC client — Goal: provide a bounded, timeout-safe newline JSON-RPC client over generic Tokio streams — Files: `crates/usage-sources/src/codex/rpc.rs` and duplex-stream tests — Acceptance: request/response correlation, notification forwarding, server-request `-32601` replies, timeout removal, disconnect draining, and bounded external text follow §8.1 — Verify: red/green duplex tests plus standard workspace gates — Result: 4 duplex tests pass; concurrent responses correlate by ID out of order, five-second paused-time expiry removes the pending entry, server requests receive exact `-32601`, notifications cross a bounded channel, and EOF drains pending callers; the driver caps commands/pending requests at 64, notifications at 32, and input lines at 1 MiB, with external errors/methods truncated before logs; full fmt, strict workspace Clippy, and workspace tests pass.
- [x] T5.2 Codex app-server and discovery — Goal: discover/spawn the Codex CLI without shell-PATH assumptions and normalize its limit/daily RPC data into source events — Files: `crates/usage-sources/src/codex/{discover.rs,app_server.rs}` and fake-process/RPC tests — Acceptance: documented discovery order, handshake/polling, `codex` limit filtering, daily usage, `-32601` Unsupported, PID tracking, stderr draining, cancellation, and restart policy follow §8.1 — Verify: deterministic fakes plus the T5.6 live probe where available — Result: 11 protocol/discovery/process tests plus one restart-policy unit test pass; discovery follows override→fixed GUI paths→bounded three-second shell lookup, the session performs handshake/full/daily/push reads and maps rate-limit `-32601` to a fixed Unsupported stop, while the owner exposes its PID, drains stderr to bounded 2 KiB lines, reaps on cancellation, emits the exact rollout-fallback degradation, and applies 5 s→5 min restarts with a ten-failure explicit-retry stop; full fmt, strict workspace Clippy, and native workspace tests pass.
- [x] T5.3 Codex rollout source — Goal: scan, watch, tail, prefilter, and parse recent Codex session JSONL without duplicates — Files: `crates/usage-sources/src/codex/rollout.rs` and temp-session tests — Acceptance: Appendix A.1 emits full readings and token deltas, premium emits tokens only, activity triggers the scheduler, and initial/watch paths are bounded — Verify: append tests under a synthetic `sessions/2026/09/23/` plus standard gates — Result: 2 source tests and 6 shared-tailer tests pass; a bounded/cancellable recursive scan keeps only the 4,096 newest files modified within eight days, native changes debounce through the 256-entry watcher, Appendix A.1 emits one full reading plus 211,554 tokens, a watched `premium` append emits activity and only its 1,000-token delta, and the app-server deadline is triggered; canonical scan/watch paths prevent duplicate replay, both scan and watcher processing recheck the eight-day mtime gate, rotation resets cumulative parser state, and partial-line memory is capped at 16 MiB/file and 64 MiB per source; full fmt, strict workspace Clippy, and native workspace tests pass.
- [x] T5.4 Claude bridge installer — Goal: install/uninstall the bundled bridge semantically while preserving every unrelated user setting — Files: `crates/usage-sources/src/claude/bridge_install.rs` and tempdir tests — Acceptance: all six T5.4 preservation/error cases, atomic writes, backup/state/config records, permissions, and no overwrite of invalid JSON follow §8.3 — Verify: byte/value comparisons in tempdirs plus standard gates — Result: all 6 mandated scenarios plus explicit-null restoration pass in 7 tempdir tests; the installer streams version comparison/copy into a stable chmod-755 path, preserves settings mode and every unrelated/future key, chains an earlier command, writes synced same-directory atomic state/config/settings, keeps a manual backup, is idempotent, and semantically restores only while the installed command remains ours; invalid/non-object settings remain byte-identical. D-010 adds a presence bit so absent and JSON-null prior values are both lossless. Full fmt, strict workspace Clippy, native workspace tests, and the direct dependency audit pass.
- [x] T5.5 Claude sources and effectiveness — Goal: ingest bridge limits and local history, and derive behavioral bridge effectiveness — Files: `crates/usage-sources/src/claude/{bridge_source.rs,history.rs,effectiveness.rs}` and injected-clock/tempdir tests — Acceptance: bridge writes produce full readings/activity, logs produce dedupable tokens/activity, and Unverified→Confirmed/LikelyOverridden transitions follow §8.3 — Verify: paused-time state tests and synthetic watcher/tail fixtures — Result: 3 injected/paused-time/burst state tests and 4 source integration tests pass; a disabled bridge emits the exact NotConfigured hint, oversized bridge input becomes a retryable source error without an unbounded read, an existing bridge envelope plus atomic native-watcher replacement each emit full Claude readings/activity, recursive history scan/tail emits stable duplicate keys and watched activity, and post-install assistant usage crosses the strict >10-minute LikelyOverridden boundary before a later bridge write recovers Confirmed. The sources register watchers before scanning/reading, cap bridge input at 4 MiB even if it grows between metadata and read, share the bounded 4,096-file/eight-day scanner and 64 MiB per-source tail budget, reject pre-install envelopes, and await the bounded effectiveness actor so a burst larger than its 32-entry queue cannot drop the newest evidence; full fmt, strict workspace Clippy, and native workspace tests pass.
- [x] T5.6 All-source probe — Goal: run enabled sources with the store and print derived snapshots as pretty JSON for live comparison — Files: `crates/usage-sources/examples/probe.rs` — Acceptance: fixture/temp mode is automatable; live Codex/Claude checks never expose secrets and unavailable interactive checks move to handover exactly — Verify: `cargo run -p usage-sources --example probe` plus documented live outcomes — Result: `cargo run -q -p usage-sources --example probe -- --fixture` passes using only a temp root and fake executable; it runs the store/scheduler/effectiveness actors plus all four v1 sources, prints pretty snapshots through Claude 23.5/41.2 and Codex rollout 1% to authoritative fake app-server 2%, proves both local histories are non-empty and the bridge is Confirmed, then cancels and joins every task within three seconds. Live mode is implemented as a bounded 30-second run and prints no credentials or raw provider input, but was not executed because reading the real `~/.codex`/`~/.claude` trees is a §0 hard stop; exact live Codex `/status` and Claude-prompt comparisons are carried to handover.

### Phase review and commit

- [x] Goal: leave M5 cancellation-safe, fallback-capable, fixture-proven, and ready for Tauri wiring — Files: Phase 6 changes only — Acceptance: source errors become statuses, all tasks/processes/watchers are owned and bounded, logs truncate external text, tests touch no real user data, and v1 contains no OAuth implementation — Verify: fmt, strict workspace Clippy/tests, source coverage/resource/dependency/diff review, probe, commit, `git log -1 --stat`, clean status — Result: all automated gates through the staged review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · workspace Clippy with all targets/features and `-D warnings` ✅ · workspace tests with all features ✅ (27 M5-focused tests; none skipped) · native FSEvents tests ✅ outside the command sandbox · fixture all-source probe ✅ · `usage-sources` coverage 78.86% lines (no M5 threshold) · `usage-core` coverage 95.57% lines (≥90%) · UI typecheck/lint/build ✅ · direct dependency and no-`reqwest`/no-`security-framework` feature-tree audits ✅ · `git diff --cached --check` ✅.
- Diff review: every M5 source and test file was re-read; RPC messages/queues/maps, recent scans, tracked files, partial lines, bridge JSON, stderr, installer copies, and probe state reads are bounded. Every process/watcher/source/actor has one cancellation owner, the probe joins within three seconds, source failures preserve last-good data through fixed status messages, and external log text is truncated. Review/coverage caught and fixed a scheduler deadline race, stale queued FSEvents bypassing the eight-day scan, loss-prone effectiveness `try_send`, and a bridge-read metadata/read race; the final actor awaits its bounded queue, a 64-event test proves the latest evidence survives, and `AsyncReadExt::take` enforces the 4 MiB bridge cap during the read itself. Installer state/config modes are asserted at 0600, settings/backups preserve original modes, all production source files are below 400 lines, and no production unwrap/expect/panic/unsafe, real-user test access, unbounded channel, OAuth implementation, network dependency, or credential output remains.
- Decisions: D-010 preserves absent versus JSON-null status-line values; D-011 defines bounded live/fixture probe termination.
- New handover items: run `cargo run -p usage-sources --example probe -- --seconds 120`, compare the Codex weekly percentage with `codex /status`, then run one Claude Code prompt with the bridge enabled and confirm a Claude reading appears within about one second. This was not run because it would read real `~/.codex` and `~/.claude` data.

### Blocked (hard stops only)

- None.

## Phase 7 — Tauri shell (M6)
Status: committed (dd5c209)

### Tasks

- [x] T6.1 Settings persistence — Goal: load, sequentially migrate, validate, atomically save, and reset the versioned settings contract — Files: `src-tauri/src/settings.rs`, `src-tauri/tests/settings.rs`, `src-tauri/Cargo.toml`, and generated settings bindings — Acceptance: missing files default with onboarding incomplete; v0 migrates to v1; future schemas are read-only and byte-unchanged; corrupt JSON is renamed; every documented setting is clamped/normalized; saves are atomic at schema v1 — Verify: tests first for every §13 case, then standard workspace gates — Result: all 7 tempdir/pure tests pass after first failing on the absent module; missing/corrupt/v0/current/future/reset paths behave as specified, future-schema bytes remain untouched, thresholds/opacity/poll intervals clamp deterministically, snake_case aliases migrate into canonical camelCase, and same-directory synced writes are atomic. Full workspace tests pass (including generated ts-rs exports and native watchers), strict workspace Clippy passes after retaining the exact boolean-heavy v1 contract with a scoped lint rationale, and UI typecheck/lint pass. Decisions D-012 and D-013 record wire naming and reuse of the pinned atomic-write dependency.
- [x] T6.2 Commands and forwarding — Goal: expose the complete typed §10.3 command/event contract, wire store/scheduler/source control, and forward snapshots/alerts with a trailing-edge 250 ms throttle — Files: `src-tauri/src/{commands.rs,commands/**,runtime.rs,runtime/**,forwarder.rs,lib.rs}`, binding tests, and `ui/src/ipc.ts` — Acceptance: every command returns typed `CommandError`; dialogs use callback-to-oneshot rather than blocking APIs; shared functions power commands and tray actions; only `ipc.ts` imports Tauri APIs — Verify: command/state/forwarder tests, binding generation/diff, standard Rust gates, and UI typecheck/lint — Result: all 17 §10.3 commands and four events are represented by generated closed DTOs and one typed UI module; the native path picker bridges its callback through a oneshot, bridge mutations roll back on persistence/effectiveness failure, and tray-ready shared implementations own show/refresh/quit behavior. A single runtime supervisor owns bounded settings/store/scheduler/effectiveness/source actors plus all event/notification forwarders, restarts one source generation only on path/source/bridge changes, dynamically applies polling/alert settings, and aborts any straggler after the three-second shutdown boundary. The snapshot coalescer's paused-time test proves both a 250 ms ceiling and latest-value trailing delivery; 22 shell tests and 19 modified source/actor tests pass, the complete workspace test suite and strict Clippy pass, generated bindings are reproducible, UI typecheck/lint pass, and a repository search confirms no UI module except `ipc.ts` imports Tauri APIs. Decision D-014 records live source reconfiguration.
- [x] T6.3 Tray and popover — Goal: build the template-icon tray, exact native menu mapping, left-click popover toggle/positioning, snapshot title/icon updates, and hide-on-blur — Files: `src-tauri/src/{tray.rs,windows.rs,forwarder.rs,lib.rs}` plus three bundled tray icons — Acceptance: left/right clicks follow §10.2, title changes only when needed, thresholds select normal/warning/critical assets, and focus loss hides popover — Verify: pure tray-state tests, configuration/build checks, and manual native behavior recorded for handover if UI automation cannot exercise the macOS tray — Result: a template tray with the exact five native actions calls the shared async command implementations; positioner receives every tray event before left-release toggles the constrained `TrayBottomCenter` popover, right-click remains native-menu-only, and focus loss hides the popover. Snapshot forwarding derives the highest actual window percentage, changes title/icon independently only when needed, and selects three distinct bundled 32×32 RGBA PNGs at the 75/90 thresholds. Two pure presentation tests, icon format/hash inspection, `cargo check -p how-is-it`, and the repository diff check pass; macOS click/menu/focus behavior is reserved for the native handover checklist because it requires GUI interaction.
- [x] T6.4 Window lifecycle — Goal: implement main hide-on-close, first-launch onboarding, widget visibility/variant sizing/position persistence/edge snap, and activation policy — Files: `src-tauri/src/{windows.rs,settings/actor.rs,commands.rs,runtime.rs,lib.rs}` and focused tests — Acceptance: exactly three windows; main is draggable through the capability/UI contract and never exits on close; widget sizes/radii/position follow variant; moved state is debounced 500 ms; edge snapping uses 24 pt detection and 12 pt inset — Verify: pure geometry/state tests, Tauri build checks, and native behaviors moved to handover where human interaction is required — Result: the runtime-owned lifecycle task applies Accessory/Regular activation policy, emits and shows first-launch onboarding, preserves main on close, sizes/restores/shows the always-on-top widget from settings, and consumes latest-only move events after a 500 ms trailing debounce. Snapping uses the current monitor's Tauri work area in logical points, a 24 pt edge test, and a 12 pt inset before atomically persisting physical coordinates; native show/hide commands and the tray persist widget visibility without overwriting concurrent settings, while future-schema read-only mode remains usable without writes. Three pure size/geometry tests and the expanded actor test pass, including negative monitor coordinates and a no-event/no-write same-position guard; config audit confirms exactly `main`, `popover`, and `widget`, the toolbar drag marker and capability are present, package Clippy/tests and diff checks pass. Glass radius is reapplied by the M7 UI bootstrap on the existing `settings-changed` event; manual close/drag/snap/Dock behavior is reserved for handover.
- [x] T6.5 Plugins, logging, and sidecar packaging — Goal: register all six required plugins, daily seven-file logging, launch-at-login synchronization, sidecar externalBin, and least-privilege capabilities — Files: `src-tauri/{Cargo.toml,tauri.conf.json,capabilities/default.json,src/lib.rs,src/logging.rs}`, `ui/package.json`, and lockfiles — Acceptance: plugin versions/features match §4; dialog/opener remain Rust-only; logging writes to the documented directory; the configured sidecar exists for the native target — Verify: `cargo tauri build --debug` or the closest non-interactive build, `cargo tauri dev` only when safely automatable, sidecar/config schema audits, dependency tree, and all standard gates — Result: all six pinned plugins are registered, only the four explicitly required frontend plugin defaults are granted, and dialog/opener remain reachable solely through typed Rust commands. A non-ANSI info subscriber writes daily `how-is-it` files under the single canonical log path, retains seven files, and holds its non-blocking flush guard for the app lifetime; the LaunchAgent is converged at startup and only changed when actual state differs. The native sidecar hook built an executable arm64 binary and `cargo tauri build --debug --bundles app` produced `How Is It.app` with arm64 main/sidecar executables, sidecar mode 0755, bundle ID `dev.howisit.app`, and minimum macOS 26.0; the broader default bundle command built the app but its optional DMG script failed in the headless environment. Capability schema validation happened during the successful app bundle, direct dependency/version and externalBin audits pass, and `cargo tauri dev` is deferred because launching it would immediately read the real Codex/Claude trees, a §0.1 hard stop.

### Phase review and commit

- [x] Goal: leave M6 fully wired, typed, bounded, cancellation-safe, and ready for the approved UI — Files: Phase 7 changes only — Acceptance: startup/shutdown order, settings, every command/event, tray/windows, plugins/logging/autostart, and sidecar configuration match §§9.3, 10, and 12 — Verify: full clean Rust/UI/build checks, resource/dependency/config/diff review, commit, `git log -1 --stat`, and clean status — Result: all automated M6 gates through the staged review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · strict workspace Clippy with all targets ✅ · complete workspace tests ✅ outside sandbox for native FSEvents (all pass, none skipped) · UI typecheck/lint/build ✅ · generated binding regeneration ✅ · package dependency/features audit ✅ · exact command/event/capability/config audits ✅ · native debug `.app` bundle ✅ · bundled architecture/mode/plist audit ✅ · `git diff --check` ✅.
- Diff review: every M6 production and test source was re-read; all 17 commands use typed arguments/results and safe public errors, only `ui/src/ipc.ts` imports Tauri APIs, external output is bounded before logs, and callback/process/file work never blocks an async executor. Store/settings/scheduler/effectiveness/sources/window events and forwarders have one cancellation owner, bounded channels or latest-value watches, and a three-second drain followed by explicit abort/join. Review caught and fixed an initially detached outer supervisor timeout, unnecessary autostart rewrites, a widget settings feedback loop, and settings-write rollback gaps. Every production Rust file is below 400 lines, with no production unwrap/expect/panic/unsafe or unbounded channel; bridge mutation remains semantic and real user data was not read.
- Decisions: D-012, D-013, D-014.
- New handover items: exercise tray left/right clicks and popover blur; close/drag/snap all widget variants across monitors; toggle Dock/login behavior; inspect Liquid Glass and notification permission behavior; run a real-data `cargo tauri dev` smoke only with owner consent; the optional debug DMG script can be retried in an interactive macOS session. The `.app` itself builds successfully and is the M6 packaging artifact.

### Blocked (hard stops only)

- None.

## Phase 8 — UI (M7)
Status: in progress

### Tasks

- [x] T7.1 Visual foundations and demo — Goal: split the approved token source into production styles, implement the layered `NeonBar`, bootstrap native glass, and expose a debug-only `#/demo` component sheet — Files: `ui/src/styles/{tokens.css,glass.css,neon.css,app.css}`, `ui/src/components/NeonBar.tsx`, `ui/src/{glass.ts,App.tsx,ipc.ts}` — Acceptance: 0/30/76/91/100/reset-pending bars reproduce `ComponentsDark.png` and `ComponentsLight.png`; all states remain legible without color; no sample data ships in release behavior — Verify: exact `bar()`/token audit, typecheck/lint/build, and same-size dark/light screenshots — Result: the verbatim dark/light variables are split from the approved stylesheet; native glass applies radius 0/18/36/24/12 per window and falls back through `.no-glass`; the React bar renders track, reflection, gradient fill, core, head, value-scaled bloom, 100% halo/pulse, width-based 1.2 s entrance and 600 ms updates, visible percentages, threshold glyphs/labels, tooltips, and reduced-motion behavior. Strict typecheck/lint/build pass; dark/light browser screenshots and the accessibility tree were reviewed; the production bundle contains none of the debug route's sample labels or timestamp.
- [x] T7.2 Usage cards and popover — Goal: implement provider identity, countdown/status semantics, overview cards, and the complete 340×420 popover action surface — Files: focused components/hooks/styles plus `ui/src/screens/Popover.tsx` and development-only visual fixtures — Acceptance: cards match `Main.png`/`States*.png`; popover matches `MenuBar*.png`, fits without scrolling, and every footer action calls the typed IPC wrapper — Verify: focused interaction/accessibility checks, typecheck/lint/build, and dark/light screenshots — Result: provider cards now derive normal, threshold, limit, loading, not-configured, degraded, stale, single-window, reset-pending, unsupported, auth-expired, and error treatments from generated domain types; one shared `useNow` interval drives all reset/updated labels; sparklines, plan labels, status chips, and text-plus-symbol thresholds follow the artboards. The exact 340×420 popover renders both compact providers without scrolling and wires Refresh, Dashboard, Widget, Settings, and Quit through typed IPC. Native-size dark/light popover and 720×520 overview renders plus the 13-state accessibility tree were reviewed; typecheck/lint/build and diff/line-size/import audits pass; development fixtures are isolated behind a lazy dev module and absent from the production bundle.
- [x] T7.3 Dashboard and history — Goal: add the hash router, overview/detail toolbar, exact hourly/daily charts, stat row, scope/as-of labels, and live snapshot/history hooks — Files: focused hooks/components/screens under `ui/src/` — Acceptance: `#/`, `#/claude`, and `#/codex` match all overview/detail artboards; charts never invent a quota or mix scopes — Verify: route/data/accessibility checks, typecheck/lint/build, and same-size dark/light screenshots — Result: the main window now uses a typed, dependency-free hash router synchronized with native `navigate` events; the live snapshot hook fetches, subscribes, unlistens safely across async mount races, and reports focus/visibility, while provider history performs a trailing refresh at no more than once per 60 seconds. The exact overview and Claude/Codex detail compositions render a native drag toolbar, 24 local-hour bars, seven scoped daily bars, per-series stale “as of” disclosure, peak and average tokens, and projection text derived exclusively from `History.projection`; there is no token quota or pace line. All six 720×520 dark/light route renders were reviewed against the artboards, a clean browser session confirmed hash navigation, 31 labelled/focusable chart bars and no console warnings, strict typecheck/lint/build pass, files remain below 400 lines, direct Tauri imports remain isolated to `ipc.ts`, and mock histories are absent from the production bundle.
- [x] T7.4 Widget, Settings, and Onboarding — Goal: implement all three widget variants and hover controls plus every settings pane and the three-step onboarding flow — Files: focused screens/components under `ui/src/` — Acceptance: `Widget*.png`, every `Settings*.png`, and `Onboarding*.png` have working typed actions and exact route/window behavior — Verify: control/tab-order/read-only checks, typecheck/lint/build, and dark/light screenshots — Result: the native widget now renders exact Pill, Stack, and Mini layouts at the M6-controlled window size; Pill/Stack replace their content with close, persisted-opacity, and expand controls inside the window bounds on hover/focus, while Mini swaps its two bars for inline percentages. Settings is a five-pane route backed by the complete versioned settings object, Codex detection/native Browse, Claude bridge install/uninstall/effectiveness, display/widget controls, sorted threshold toggles, polling intervals, live source diagnostics, log reveal, app version, and an explicit future-schema reset banner with every ordinary input disabled. Onboarding implements Codex detection/path choice, the complete Claude bridge disclosure with Enable/Skip, and user-triggered notification permission followed by completion/navigation; its end-to-end browser flow reaches Overview. Every widget, settings pane/read-only state, and onboarding step was reviewed in dark and light at app size, settings switch/select mutations were exercised, strict typecheck/lint/build and import/diff/file-size audits pass, and fixture paths/versions/gallery code are absent from the production bundle.
- [x] T7.5 Edge states and accessibility — Goal: complete every §11.4 state and prove light/dark, reduced-motion, reduced-transparency fallback, keyboard, and grayscale behavior — Files: final UI/style adjustments and decision/handover records only — Acceptance: `States*.png`, `SettingsReadOnlyDark.png`, and `Accessibility.png` are represented without hard-coded visual values outside styles — Verify: complete route/state screenshot matrix, contrast/semantic DOM audit, production build, and native-only checks recorded for handover — Result: the live dashboard now consumes Claude bridge effectiveness and renders the required `LikelyOverridden` warning with the §8.3 hint, a dimmed session value, and Settings action; the state gallery has all 15 designed cells including a text/icon threshold legend. A dedicated accessibility route represents opaque fallback, static reduced-motion behavior, contrast guidance, and grayscale threshold semantics; all non-token color literals were removed from component styles. Exact-size `StatesDark`, `StatesLight`, `SettingsReadOnlyDark`, and `Accessibility` renders were reviewed; 36 dark/light route combinations have exactly one non-nested main, unique IDs, labelled controls, and valid progressbar ARIA, with zero console warnings. Keyboard order skips disabled read-only controls, media emulation computes no motion and opaque reduced-transparency surfaces, and token contrast bottoms out at 4.98:1 dark / 5.31:1 light. Typecheck, lint, production build, debug-fixture exclusion, direct-import, file-size, and diff checks pass. D-015 records the native WKWebView preference check for handover.

### Phase review and commit

- [x] Goal: leave M7 pixel-reviewed, typed, accessible, dependency-minimal, and fully connected to the M6 shell — Files: Phase 8 changes only — Acceptance: every UI artboard/state is represented; no fake chrome/wallpaper/sample production data; no Tauri imports outside `ipc.ts`; all automated checks pass — Verify: UI and workspace gates, screenshot review, generated binding diff, staged diff review, commit, `git log -1 --stat`, and clean status — Result: all automated gates and the staged review pass; commit follows this record.

### Review

- Checks: `cargo fmt --all -- --check` ✅ · strict workspace Clippy ✅ · complete workspace tests ✅ (native FSEvents suite rerun outside the command sandbox; all pass, none skipped) · UI typecheck/lint/production build ✅ · generated bindings unchanged ✅ · 36-route dark/light semantic matrix and exact-size screenshot review ✅ · reduced-motion/transparency emulation and token-contrast audit ✅ · production fixture/direct-import/color-literal audits ✅ · staged diff check ✅.
- Diff review: all 51 M7 files were reviewed against §11 and the design package. Every screen, route, edge state, widget size and action is connected to generated types and typed IPC; debug data remains behind the development-only lazy route and is absent from the production bundle. Review fixed an async native-glass cleanup race, an incorrect “Today” fallback for old history, unknown-window labelling, invisible stale threshold glyphs, duplicate opacity writes, indefinite history loading after errors, a transient fake production version, and Codex account fixture dates. No fake chrome/wallpaper, network/storage dependency, raw component color literal, direct Tauri import outside `ipc.ts`, generated-binding drift, file over 400 lines, TODO, or unrelated contract/design edit remains.
- Decisions: D-015 records the system Reduce Transparency query plus opaque failure fallback.
- New handover items: inspect native Liquid Glass, dark/light switching and Reduce Transparency in WKWebView; compare each real window and widget over the desktop; confirm the native notification prompt appears only after the onboarding action. Browser-emulated screenshots and semantics already pass.

### Blocked (hard stops only)

- None.
